
<script>
  // var height = $('.content').height(); 
  // $('.sidebar').height(height);
  // $('.expandSidebar').height(height);
  // $("window").resize(function () {
	  // var height = $('.content').height(); 
      // $('.sidebar').height(height);
      // $('.expandSidebar').height(height);
  // })
</script>
</body>
</html>
